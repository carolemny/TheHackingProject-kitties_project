<div class="row">
  <div class="col-md-5 m-auto">

    <h2 class="mb-3 text-center">Renvoyer les instructions de déverrouillage</h2>
    
    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "form-signin" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field form-group">
        <%= f.label :email, "Email" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
      </div>
      <br>

      <div class="actions text-center">
        <%= f.submit "Renvoyer les instructions de déverrouillage", class: "my-btn my-btn col-md-12" %>
      </div>
    <% end %>
    <br>

    <%= render "devise/shared/links" %>

  </div>
</div>